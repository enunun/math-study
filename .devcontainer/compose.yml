services:
  app:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    container_name: math-study-container
    volumes:
      - ..:/workspaces:cached  
    working_dir: /workspaces
    command: ["sleep", "infinity"]

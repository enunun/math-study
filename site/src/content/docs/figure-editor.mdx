---
title: 図の作成
description: 座標軸，グラフ，曲線，点，曲面などを組み合わせて，平面や空間の図をブラウザの上で作る．図は，シーンのJSONと，TikZとして取り出せる．
tableOfContents: false
---

import { FigureEditor } from '@/components/figure-editor/figure-editor';

サイトの記事で使っている図と同じ形式の図を，ブラウザの上で作る．作った図は，シーンのJSONとTikZとして取り出せる．編集中の図は，このブラウザに保存される．

<FigureEditor client:load />

## 使い方

- 「見本」から出発するか，「空の図」にオブジェクトを足して作る．
- 「フォーム」で，図の種類と範囲，オブジェクトを設定する．オブジェクトは，一覧から選んで設定し，並びの変更と削除もできる．
- 点は，その点を使うベクトルや線分より前に置く．並びは，描く順と，参照の順である．
- 数を入れる欄には，式も書ける．式では，`+`，`-`，`*`，`/`，`^`と，`sin`，`cos`，`sqrt`，`exp`，`log`などの関数，定数の`pi`と`e`が使える．`2x`のような省略は使えず，`2*x`と書く．
- 「JSON」では，シーンを直接編集できる．「JSONを読み込む」と「JSONを書き出す」で，ファイルとして出し入れする．
- 「TikZを書き出す」で，図の断片(`.tikz`)を得る．「単体の文書を書き出す」で，そのままコンパイルできるLuaLaTeXの文書(`.tex`)を得る．

## TikZを自分のTeX文書に入れる

断片(`.tikz`)を，`tikzpicture`環境ごと，自分の文書に貼る．プリアンブルには，次の3行を書く．

```latex
\usepackage{tikz}
\usetikzlibrary{arrows.meta}
\usepackage{amsmath}
```

日本語のラベルを使うときは，LuaLaTeXと，LuaTeX-jaを使う．図の見た目は，このページのプレビューと，ほぼ同じになる．ラベルの文字の位置は，プレビューと少しずれることがある．
